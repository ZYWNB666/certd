# 🐳 Certd 生产环境部署指南（VIP破解版）

## 📦 快速开始

### 方法 1: 使用 Docker Compose（推荐）

```bash
# 1. 构建并启动
docker-compose -f docker-compose.production.yml up -d

# 2. 查看日志
docker-compose -f docker-compose.production.yml logs -f

# 3. 停止服务
docker-compose -f docker-compose.production.yml down
```

### 方法 2: 手动构建镜像

```bash
# 1. 构建镜像
docker build -t certd-vip:latest -f Dockerfile.production .

# 2. 运行容器
docker run -d \
  --name certd-vip \
  -p 7001:7001 \
  -p 7002:7002 \
  -v $(pwd)/data:/app/data \
  -e TZ=Asia/Shanghai \
  --restart unless-stopped \
  certd-vip:latest

# 3. 查看日志
docker logs -f certd-vip
```

---

## ✅ 验证部署成功

### 1. 检查服务状态

```bash
# 查看容器状态
docker ps | grep certd

# 查看健康检查
docker inspect certd-vip | grep Health -A 10

# 查看日志（重要！）
docker logs certd-vip | grep -E "MOCK|授权信息|Certd已启动"
```

### 2. 验证 VIP 破解

日志中应该看到：

```
[MOCK] Verifying license: ...
授权信息:comm,2099-12-31  ← 商业版，2099年过期
Certd已启动
```

### 3. 访问服务

```
HTTP:  http://your-server-ip:7001
HTTPS: https://your-server-ip:7002
```

---

## 🎯 VIP 功能验证

登录后台，测试以下功能：

### ✅ 站点监控
- 路径：监控 → 站点监控
- 验证：可以添加多个站点（免费版限制1个）

### ✅ 通知功能
- 路径：设置 → 通知配置
- 验证：可以使用专业版通知类型（企微、钉钉、飞书等）

### ✅ 流水线
- 路径：证书 → 流水线
- 验证：可以创建无限个流水线

### ✅ VIP 状态
- 位置：右上角金色 VIP 图标
- 验证：显示商业版，2099年过期

---

## 🔧 高级配置

### 使用外部数据库

#### PostgreSQL

编辑 `docker-compose.production.yml`，取消注释 PostgreSQL 相关配置：

```yaml
environment:
  - certd_flyway_scriptDir=./db/migration-pg
  - certd_typeorm_dataSource_default_type=postgres
  - certd_typeorm_dataSource_default_host=postgres
  - certd_typeorm_dataSource_default_port=5432
  - certd_typeorm_dataSource_default_username=certd
  - certd_typeorm_dataSource_default_password=your_secure_password
  - certd_typeorm_dataSource_default_database=certd
```

#### MySQL

```yaml
environment:
  - certd_flyway_scriptDir=./db/migration-mysql
  - certd_typeorm_dataSource_default_type=mysql
  - certd_typeorm_dataSource_default_host=mysql
  - certd_typeorm_dataSource_default_port=3306
  - certd_typeorm_dataSource_default_username=certd
  - certd_typeorm_dataSource_default_password=your_secure_password
  - certd_typeorm_dataSource_default_database=certd
```

### 配置反向代理（Nginx）

```nginx
server {
    listen 80;
    server_name certd.yourdomain.com;

    # 强制 HTTPS
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name certd.yourdomain.com;

    ssl_certificate /path/to/cert.pem;
    ssl_certificate_key /path/to/key.pem;

    location / {
        proxy_pass http://localhost:7001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

---

## 📊 监控和维护

### 查看资源使用

```bash
# CPU 和内存使用
docker stats certd-vip

# 磁盘使用
du -sh data/
```

### 备份数据

```bash
# 备份数据目录
tar -czf certd-backup-$(date +%Y%m%d).tar.gz data/

# 定期备份（crontab）
0 2 * * * tar -czf /backup/certd-backup-$(date +\%Y\%m\%d).tar.gz /path/to/certd/data/
```

### 升级

```bash
# 1. 备份数据
tar -czf certd-backup-$(date +%Y%m%d).tar.gz data/

# 2. 停止服务
docker-compose -f docker-compose.production.yml down

# 3. 拉取最新代码
git pull

# 4. 重新构建
docker-compose -f docker-compose.production.yml build --no-cache

# 5. 启动服务
docker-compose -f docker-compose.production.yml up -d

# 6. 验证
docker logs certd-vip | grep -E "MOCK|授权信息"
```

---

## 🐛 故障排查

### 容器无法启动

```bash
# 查看详细错误
docker logs certd-vip

# 检查端口占用
netstat -tlnp | grep -E '7001|7002'

# 重新构建（清除缓存）
docker-compose -f docker-compose.production.yml build --no-cache
```

### VIP 功能未生效

检查日志中是否有 `[MOCK]` 标记：

```bash
docker logs certd-vip | grep MOCK
```

如果没有，说明 Mock 模块未正确构建，需要重新构建镜像。

### 数据库连接失败

```bash
# 检查数据库服务状态
docker ps | grep -E 'postgres|mysql'

# 测试数据库连接
docker exec -it certd-postgres psql -U certd -d certd
# 或
docker exec -it certd-mysql mysql -u certd -p
```

---

## 🔒 安全建议

1. **修改默认密码**：首次登录后立即修改管理员密码
2. **使用 HTTPS**：配置 SSL 证书或使用反向代理
3. **限制访问**：使用防火墙限制 7001/7002 端口访问
4. **定期备份**：每天备份 `data/` 目录
5. **更新系统**：定期更新 Docker 镜像和宿主机系统

---

## 📝 环境要求

- **Docker**: >= 20.10
- **Docker Compose**: >= 2.0
- **CPU**: >= 2核
- **内存**: >= 2GB
- **磁盘**: >= 10GB

---

## 🎉 常见问题

### Q: 如何重置管理员密码？

A: 编辑 `docker-compose.production.yml`，设置：
```yaml
- certd_system_resetAdminPasswd=true
```
重启容器后密码会重置为 `123456`，然后记得改回 `false`。

### Q: 数据存储在哪里？

A: 所有数据存储在 `./data/` 目录，包括：
- 数据库文件（SQLite）
- 证书文件
- 配置文件
- 日志文件

### Q: 如何确认破解成功？

A: 查看日志：
```bash
docker logs certd-vip | grep "授权信息"
```
应该显示：`授权信息:comm,2099-12-31`

---

## 📞 技术支持

如有问题，请检查：
1. Docker 日志：`docker logs certd-vip`
2. VIP 破解完整指南.md
3. BYPASS_VIP.md

---

**部署时间**: $(date)  
**版本**: 1.37.10-vip  
**破解状态**: ✅ 已启用  
**到期时间**: 2099-12-31

